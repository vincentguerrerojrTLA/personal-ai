const RAW_DATA_URL='https://raw.githubusercontent.com/vincentguerrerojrTLA/personal-ai/main/data/roadmap.json';
const RAW_PHASE_URL='https://raw.githubusercontent.com/vincentguerrerojrTLA/personal-ai/main/data/phases.json';
const LOCAL_DATA_URL='./data/roadmap.json';
const LOCAL_PHASE_URL='./data/phases.json';
const state={data:null,phaseData:null,screen:'home',filter:'ALL',query:'',selected:null,selectedPhase:null,connected:false,lastFetch:null};
const $=(s,r=document)=>r.querySelector(s);const $$=(s,r=document)=>[...r.querySelectorAll(s)];
const esc=(v='')=>String(v).replace(/[&<>'"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[m]));
const fmtTime=iso=>{try{return new Date(iso).toLocaleString([],{month:'short',day:'numeric',hour:'numeric',minute:'2-digit'})}catch{return iso}};
const statusClass=s=>String(s||'').toLowerCase().replace(/[^a-z0-9]+/g,'-');
async function getJson(url){const r=await fetch(url,{cache:'no-store'});if(!r.ok)throw new Error(`HTTP ${r.status}`);return r.json()}
async function loadData(initial=false){
  try{
    const stamp=Date.now();
    const [roadmap,phases]=await Promise.all([getJson(initial?LOCAL_DATA_URL:`${RAW_DATA_URL}?t=${stamp}`),getJson(initial?LOCAL_PHASE_URL:`${RAW_PHASE_URL}?t=${stamp}`)]);
    const old=state.data?.meta?.updated_at;state.data=roadmap;state.phaseData=phases;state.connected=true;state.lastFetch=new Date();
    if(!state.selected)state.selected=roadmap.current.focus_code;if(!state.selectedPhase)state.selectedPhase=phases.current_phase;
    renderAll();updateLive();if(old&&old!==roadmap.meta.updated_at)flashUpdate();
  }catch(err){state.connected=false;state.lastFetch=new Date();updateLive();console.warn('Live refresh failed:',err)}
}
function flashUpdate(){const el=$('#liveIndicator');if(el)el.animate([{boxShadow:'0 0 0 rgba(98,231,196,0)'},{boxShadow:'0 0 28px rgba(98,231,196,.55)'},{boxShadow:'0 0 0 rgba(98,231,196,0)'}],{duration:1000})}
function navigate(screen,selected=null){state.screen=screen;if(screen==='roadmap'&&selected)state.selected=selected;if(screen==='phases'&&selected)state.selectedPhase=selected;$$('.screen').forEach(x=>x.classList.toggle('active',x.dataset.screen===screen));$$('.nav button,.mobile-drawer button').forEach(x=>x.classList.toggle('active',x.dataset.nav===screen));if(screen==='roadmap')renderRoadmap();if(screen==='phases')renderPhases();history.replaceState(null,'',selected?`#${screen}/${encodeURIComponent(selected)}`:`#${screen}`);window.scrollTo({top:0,behavior:'smooth'});$('#mobileDrawer')?.classList.remove('open')}
function parseHash(){const raw=location.hash.slice(1);if(!raw)return;const [screen,item]=raw.split('/');if(['home','phases','roadmap','systems','agents','integrations','activity'].includes(screen)){state.screen=screen;if(item){if(screen==='phases')state.selectedPhase=decodeURIComponent(item);else state.selected=decodeURIComponent(item)}}}
function renderAll(){if(!state.data)return;renderHeader();renderHome();renderPhases();renderRoadmap();renderSystems();renderAgents();renderIntegrations();renderActivity();navigate(state.screen,state.screen==='phases'?state.selectedPhase:state.selected)}
function renderHeader(){const d=state.data;$('#headerRelease').textContent=d.current.release;$('#brandVersion').textContent=`${d.current.release} · PHASE COMMAND CENTER`}
function renderHome(){const d=state.data,c=d.current;$('#heroTitle').innerHTML='THE BUILD<span>IN MOTION</span>';$('#heroCopy').textContent=c.summary;$('#principles').innerHTML=c.principles.map((p,i)=>`<div class="principle"><b>0${i+1}</b>${esc(p)}</div>`).join('');$('#releaseName').textContent=c.release;const cp=currentPhase();$('#focusTitle').textContent=cp?`${cp.number} · ${cp.title}`:c.focus_title;$('#focusSummary').textContent=cp?.summary||(d.roadmap.find(x=>x.code===c.focus_code)||{}).summary||'';$('#foundationLabel').textContent=`${c.foundation_complete} / ${c.foundation_total}`;$('#foundationBar').style.width=`${Math.round(c.foundation_complete/c.foundation_total*100)}%`;$('#v1Label').textContent=`${c.v1_complete} COMPLETE · ${c.v1_next} NEXT`;$('#v1Bar').style.width=`${Math.round(c.v1_complete/Math.max(4,c.v1_complete+1)*100)}%`;$('#statusGrid').innerHTML=d.status.map(s=>`<div class="status-mini tone-${esc(s.tone)}"><span>${esc(s.label)}</span><b>${esc(s.value)}</b></div>`).join('');$('#homeUpdated').textContent=fmtTime(d.meta.updated_at);renderHomePhaseStrip()}
function currentPhase(){return state.phaseData?.phases?.find(x=>x.code===state.phaseData.current_phase)}
function renderHomePhaseStrip(){const wrap=$('#homePhaseStrip');if(!wrap||!state.phaseData)return;wrap.innerHTML=state.phaseData.phases.map(p=>`<button class="phase-mini ${p.code===state.phaseData.current_phase?'current':''}" data-phase-mini="${esc(p.code)}"><span class="phase-mini-num">${esc(p.number)}</span><span class="phase-mini-copy"><b>${esc(p.title)}</b><small>${esc(p.range)}</small></span><span class="badge ${statusClass(p.status)}">${esc(p.status)}</span></button>`).join('');$$('[data-phase-mini]',wrap).forEach(b=>b.onclick=()=>navigate('phases',b.dataset.phaseMini))}
function renderPhases(){const rail=$('#phaseRail'),wrap=$('#phaseSections');if(!rail||!wrap||!state.phaseData)return;const phases=state.phaseData.phases;rail.innerHTML=phases.map(p=>`<button class="phase-rail-item ${p.code===state.selectedPhase?'active':''} ${p.code===state.phaseData.current_phase?'current':''}" data-phase-jump="${esc(p.code)}"><span>${esc(p.number)}</span><b>${esc(p.title)}</b><small>${esc(p.status)}</small></button>`).join('');wrap.innerHTML=phases.map(p=>phaseSection(p)).join('');$$('[data-phase-jump]',rail).forEach(b=>b.onclick=()=>{state.selectedPhase=b.dataset.phaseJump;renderPhases();document.getElementById(`phase-${state.selectedPhase}`)?.scrollIntoView({behavior:'smooth',block:'start'});history.replaceState(null,'',`#phases/${encodeURIComponent(state.selectedPhase)}`)});$$('[data-roadmap-code]',wrap).forEach(b=>b.onclick=()=>navigate('roadmap',b.dataset.roadmapCode));if(state.screen==='phases'&&state.selectedPhase)setTimeout(()=>document.getElementById(`phase-${state.selectedPhase}`)?.scrollIntoView({behavior:'auto',block:'start'}),20)}
function phaseSection(p){const items=p.completed?.length?p.completed:(p.targets||[]);const label=p.status==='COMPLETE'?'Completed in this phase':'Targets for this phase';return `<article class="phase-section ${p.code===state.phaseData.current_phase?'current':''}" id="phase-${esc(p.code)}"><div class="phase-number">${esc(p.number)}</div><div class="phase-main"><div class="phase-section-head"><div><div class="eyebrow">${esc(p.range)}</div><h3>${esc(p.title)}</h3></div><span class="badge ${statusClass(p.status)}">${esc(p.status)}</span></div><p class="phase-summary">${esc(p.summary)}</p><p class="phase-detail">${esc(p.detail)}</p><div class="phase-progress"><div class="phase-progress-meta"><span>PHASE PROGRESS</span><b>${esc(p.progress)}%</b></div><div class="bar"><i style="width:${Number(p.progress)||0}%"></i></div></div><div class="phase-detail-grid"><div class="phase-list"><b>${label}</b><ul>${items.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div><div class="phase-facts"><div><b>Verification</b><span>${esc(p.verification)}</span></div><div><b>Unlocks</b><span>${esc(p.unlocks)}</span></div><div><b>Next Phase</b><span>${esc(p.next)}</span></div></div></div>${p.roadmap_code?`<button class="btn" data-roadmap-code="${esc(p.roadmap_code)}">Open milestone details</button>`:''}</div></article>`}
function groupRoadmap(items){return items.reduce((a,x)=>{(a[x.phase]??=[]).push(x);return a},{})}
function roadmapFiltered(){const q=state.query.trim().toLowerCase();return state.data.roadmap.filter(x=>{const f=state.filter==='ALL'||x.status===state.filter;const hay=`${x.code} ${x.title} ${x.summary} ${x.objective} ${x.evidence}`.toLowerCase();return f&&(!q||hay.includes(q))})}
function renderRoadmap(){if(!state.data)return;const items=roadmapFiltered(),groups=groupRoadmap(items),list=$('#roadmapList');const selected=state.data.roadmap.find(x=>x.code===state.selected)||items[0]||state.data.roadmap[0];if(!items.length)list.innerHTML='<div class="empty">NO ROADMAP ITEMS MATCH THIS VIEW.</div>';else list.innerHTML=Object.entries(groups).map(([phase,arr])=>`<div class="phase-group"><div class="phase-label">${esc(phase)}</div>${arr.map(x=>`<button class="roadmap-item ${x.code===state.selected?'active':''}" data-roadmap="${esc(x.code)}"><span class="roadmap-code">${esc(x.code.replace('GATE ','G'))}</span><span class="roadmap-title">${esc(x.title)}<small>${esc(x.summary.slice(0,78))}${x.summary.length>78?'…':''}</small></span><span class="badge ${statusClass(x.status)}">${esc(x.status)}</span></button>`).join('')}</div>`).join('');$$('[data-roadmap]',list).forEach(b=>b.onclick=()=>{state.selected=b.dataset.roadmap;renderRoadmap();history.replaceState(null,'',`#roadmap/${encodeURIComponent(state.selected)}`)});renderRoadmapDetail(selected);const counts=state.data.roadmap.reduce((a,x)=>(a[x.status]=(a[x.status]||0)+1,a),{});$('#roadmapCounts').textContent=`${counts.COMPLETE||0} COMPLETE · ${counts.NEXT||0} NEXT · ${counts.PLANNED||0} PLANNED`}
function renderRoadmapDetail(x){const d=$('#roadmapDetail');if(!x){d.innerHTML='<div class="empty">SELECT A ROADMAP ITEM.</div>';return}d.innerHTML=`<div class="eyebrow">${esc(x.phase)} · ${esc(x.code)}</div><h3>${esc(x.title)}</h3><span class="badge ${statusClass(x.status)}">${esc(x.status)}</span><p class="detail-summary">${esc(x.summary)}</p><div class="detail-grid"><div class="detail-block"><b>Objective</b><span>${esc(x.objective||'Not scheduled yet.')}</span></div><div class="detail-block"><b>Unlocked capability</b><span>${esc(x.unlocked||'Pending.')}</span></div><div class="detail-block wide evidence"><b>Verification / evidence</b><span>${esc(x.evidence||'Verification begins when this milestone enters production.')}</span></div><div class="detail-block wide"><b>Next action</b><span>${esc(x.next_action||'No additional action recorded.')}</span></div></div>`}
function renderSystems(){$('#systemsGrid').innerHTML=state.data.systems.map(x=>`<article class="data-card"><span class="badge ${x.verification==='PASS'?'pass':'info'}">${esc(x.status)}</span><h3>${esc(x.name)}</h3><p>${esc(x.purpose)}</p><div class="card-facts"><div class="fact"><b>Latest verification</b><span>${esc(x.verification)}</span></div><div class="fact"><b>Safety boundary</b><span>${esc(x.boundary)}</span></div></div></article>`).join('')}
function renderAgents(){$('#agentsGrid').innerHTML=state.data.agents.map(x=>`<article class="data-card agent"><span class="badge ${x.status.includes('VALIDATED')?'complete':'info'}">${esc(x.status)}</span><h3>${esc(x.name)}</h3><p>${esc(x.scope)}</p><div class="fact"><b>Governance</b><span>Inherits protected owner rules and scoped permissions.</span></div></article>`).join('')}
function renderIntegrations(){$('#integrationsGrid').innerHTML=state.data.integrations.map(x=>`<article class="data-card"><span class="badge ${statusClass(x.status)}">${esc(x.status)}</span><h3>${esc(x.name)}</h3><p>${esc(x.detail)}</p></article>`).join('')}
function renderActivity(){const items=[...state.data.activity].sort((a,b)=>new Date(b.time)-new Date(a.time));$('#activityFeed').innerHTML=items.map(x=>`<div class="activity-entry"><div class="activity-time">${esc(fmtTime(x.time))}</div><div class="activity-type"><span class="badge ${statusClass(x.status)}">${esc(x.type)}</span></div><div class="activity-body"><strong>${esc(x.title)}</strong><p>${esc(x.detail)}</p></div></div>`).join('');$('#activityCount').textContent=`${items.length} RECORDED UPDATES`;$('#activityCurrent').textContent=state.data.current.focus_title;const cp=currentPhase();if($('#activityPhase'))$('#activityPhase').textContent=cp?`${cp.number} · ${cp.title}`:'—'}
function updateLive(){const ind=$('#liveIndicator'),text=$('#liveText'),last=$('#lastFetch'),source=$('#dataSource'),stamp=$('#sourceUpdated');if(ind)ind.classList.toggle('live',state.connected);if(text)text.textContent=state.connected?'LIVE / CONNECTED':'LOCAL FALLBACK';if(last)last.textContent=state.lastFetch?state.lastFetch.toLocaleTimeString([],{hour:'numeric',minute:'2-digit',second:'2-digit'}):'—';if(source)source.textContent=state.connected?'GitHub main / roadmap + phases':'Bundled local copy';if(stamp)stamp.textContent=state.data?fmtTime(state.data.meta.updated_at):'—'}
function bindUI(){$$('.nav button,.mobile-drawer button').forEach(b=>b.onclick=()=>navigate(b.dataset.nav));$('#menuBtn').onclick=()=>$('#mobileDrawer').classList.toggle('open');$('#viewPhases').onclick=()=>navigate('phases',state.phaseData?.current_phase);$('#openAllPhases').onclick=()=>navigate('phases',state.phaseData?.current_phase);$('#viewRoadmap').onclick=()=>navigate('roadmap',state.data?.current.focus_code);$('#viewActivity').onclick=()=>navigate('activity');$$('.filter').forEach(b=>b.onclick=()=>{state.filter=b.dataset.filter;$$('.filter').forEach(x=>x.classList.toggle('active',x===b));renderRoadmap()});$('#roadmapSearch').addEventListener('input',e=>{state.query=e.target.value;renderRoadmap()});window.addEventListener('hashchange',()=>{parseHash();navigate(state.screen,state.screen==='phases'?state.selectedPhase:state.selected)})}
function initNetwork(){const c=$('#network'),ctx=c.getContext('2d');let w,h,nodes=[];function resize(){w=c.width=innerWidth*devicePixelRatio;h=c.height=innerHeight*devicePixelRatio;c.style.width=innerWidth+'px';c.style.height=innerHeight+'px';nodes=Array.from({length:Math.min(55,Math.max(24,Math.floor(innerWidth/30)))},()=>({x:Math.random()*w,y:Math.random()*h,vx:(Math.random()-.5)*.12*devicePixelRatio,vy:(Math.random()-.5)*.12*devicePixelRatio,r:(Math.random()*1.2+.4)*devicePixelRatio}))}function frame(){ctx.clearRect(0,0,w,h);ctx.lineWidth=.55*devicePixelRatio;for(let i=0;i<nodes.length;i++){const a=nodes[i];a.x+=a.vx;a.y+=a.vy;if(a.x<0||a.x>w)a.vx*=-1;if(a.y<0||a.y>h)a.vy*=-1;for(let j=i+1;j<nodes.length;j++){const b=nodes[j],dx=a.x-b.x,dy=a.y-b.y,dist=Math.hypot(dx,dy);if(dist<150*devicePixelRatio){ctx.strokeStyle=`rgba(62,137,255,${.12*(1-dist/(150*devicePixelRatio))})`;ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke()}}ctx.fillStyle='rgba(89,217,255,.34)';ctx.beginPath();ctx.arc(a.x,a.y,a.r,0,Math.PI*2);ctx.fill()}requestAnimationFrame(frame)}addEventListener('resize',resize);resize();frame()}
parseHash();bindUI();initNetwork();loadData(true).then(()=>setTimeout(()=>loadData(false),1200));setInterval(()=>loadData(false),15000);
